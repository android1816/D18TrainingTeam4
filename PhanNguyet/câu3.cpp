#include <stdio.h>
int main (){
	int n;
	scanf("%d",&n);		// chỉnh lại bố cục code
	if (n==0){
		printf("0");
		}
	else if (n<0){ 
	printf("so am");
	}
	else printf ("so duong");
return 0;
}		
