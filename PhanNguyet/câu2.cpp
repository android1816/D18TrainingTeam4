#include <stdio.h>
int main (){
	int n;
	scanf("%d",&n);
	if (n>0){	// sai logic, sửa lỗi và chỉnh lại bố cục code
	if (n%2==0){
		printf("so chan");
		}
	else printf("so le");
	}
return 0;
}		
